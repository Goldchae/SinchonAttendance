import { THIS_WEEK, MESSAGE } from "./consts";

import {
  thisWeekAttendanceRef,
  thisWeekSecretRef,
  isStudent,
  isAttendance,
  studentIsHere,
  getThisWeekWordObject,
} from "./firebaseData";

import "./style.css";
